#pragma once

/*
 * enum class qui gere le mode dans lequel le jeu est present, je m'en suis pas trop servi au final
*/

enum class mode{
    inGame,
    inMenu,
    inHelper,
    inEndScreen,
};
